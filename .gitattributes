# Shell scripts must keep LF endings whatever a contributor's core.autocrlf is
# set to. eng/run-tests.sh runs on the Ubuntu CI runner, where CRLF would make
# the shebang fail with "bad interpreter: /usr/bin/env bash^M".
*.sh text eol=lf
